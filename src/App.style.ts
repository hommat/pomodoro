import { styled } from './styles/theme';

export const SApp = styled.div`
  text-align: center;
  margin: 0 auto;
  padding: 1em;
  max-width: 800px;
`;

export const STitle = styled.h1`
  font-size: 3.4em;
  margin: 0.4em 0;
`;
